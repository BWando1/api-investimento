# SQLite datasource configuration
quarkus.datasource.db-kind=other
quarkus.datasource.jdbc.driver=org.sqlite.JDBC
quarkus.datasource.jdbc.url=jdbc:sqlite:/data/investimento.db

# Hibernate ORM configuration
quarkus.hibernate-orm.dialect=org.hibernate.community.dialect.SQLiteDialect
quarkus.hibernate-orm.database.generation=none
quarkus.hibernate-orm.log.sql=false

# SQL migrations
quarkus.flyway.migrate-at-start=true
quarkus.flyway.locations=db/migration

# HTTP port
quarkus.http.port=8080

# OpenAPI / Swagger UI
quarkus.swagger-ui.always-include=true

# OIDC / Keycloak
quarkus.oidc.application-type=service
quarkus.oidc.auth-server-url=${OIDC_AUTH_SERVER_URL:http://localhost:8180/realms/investimentos}
quarkus.oidc.client-id=${OIDC_CLIENT_ID:api-investimento}
quarkus.oidc.credentials.secret=${OIDC_CLIENT_SECRET:}
# Roles are read from JWT at realm_access.roles (e.g. usuario, analista)
# Note: this setting only maps roles from token claims; authorization policy is defined below.
quarkus.oidc.roles.role-claim-path=realm_access/roles

# Require authentication for business endpoints only
quarkus.http.auth.permission.authenticated.paths=/perfil-risco/*,/simular-investimento*,/telemetria*,/produtos-recomendados*,/simulacoes*
quarkus.http.auth.permission.authenticated.policy=authenticated

# Public routes
quarkus.http.auth.permission.health.paths=/q/health
quarkus.http.auth.permission.health.policy=permit

quarkus.http.auth.permission.openapi.paths=/openapi,/openapi/*
quarkus.http.auth.permission.openapi.policy=permit

quarkus.http.auth.permission.swagger.paths=/q/swagger-ui,/q/swagger-ui/*
quarkus.http.auth.permission.swagger.policy=permit

# Logging
quarkus.log.level=INFO
